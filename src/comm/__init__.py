"""Communication package boundary.

Responsibilities:
- Receive raw packets from TCP and emit normalized packet objects/signals.

In scope:
- Network socket handling and packet parsing.

Out of scope:
- Signal preprocessing, plotting, feature extraction, and storage policy.
"""